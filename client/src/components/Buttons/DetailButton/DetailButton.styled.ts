import styled from 'styled-components'

export const DetailButtonStyled = styled.div`
  height: 28px;
  font-size: 0.875rem;
  background-color: var(--color-muted-background);
  color: var(--color-muted-text);
  border-radius: 6px;
  padding: 4px 12px;
`