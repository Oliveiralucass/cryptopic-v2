import styled from 'styled-components'

export const AdSectionStyled = styled.div`
    display: flex;
    height: 140px;
    padding: 20px 0;
    border-bottom: 1px solid var(--color-secondary);
    border-top: 1px solid var(--color-secondary);
    margin-top: 85px;
`